let a = 'female';
let gender = a === 'male' ? 'Luffy' : ' Nami';
console.log(gender);

let x = 90;
let sinx = x === 0 ? 0 : x === 30 ? 0.5 : x === 90 ? 1 : 'Error' ;
console.log("sin(" +x +") = " +sinx);